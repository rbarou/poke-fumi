"Inscription":"userService"
"input":
{
    "username" : "string", -> unique en BDD !
    "password" : "string"
}
"output" : "gg t'as un compte"

"en BBD":
    {
        "id":number
        "username":string
        "password":string
        "score":number
    }

"Connexion":"userService"
"input":
{
    "username" : "string",
    "password" : "string"
}

"output": "gg t'es connecté / nn c pa possible"

"en BDD" : "pas de changement"

"voir liste joueurs":"user service"
"pas d'input"
"sortie":
[
    {
        "username":string
        "score":number
    }
]

"voir liste match" : "matchService qui tape dans userService pour détails sur les joueurs"

"input":rien
"output":
[
    "matchName":string
    "type":string (privé/ partage URL)
    "status":string (enCours/termine)
]
"en BDD" : "pas de changement"

"voir détails match" : "matchService"

"input" : "Un nom de match"
"output":
{
    "matchName":string
    "type":string(privé / partage URL)
    ""
}